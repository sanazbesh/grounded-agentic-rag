# Runtime dependencies
streamlit>=1.30
PyMuPDF>=1.24
pymupdf4llm>=0.0.10
llama-cpp-python>=0.3.0
SQLAlchemy>=2.0
psycopg[binary]>=3.1
qdrant-client
# Required by DenseEmbeddingService's default SentenceTransformer backend.
# Callers may inject a custom EmbeddingBackend, but the shipped runtime path uses this package.
sentence-transformers

# Development / testing
pytest>=8.0
